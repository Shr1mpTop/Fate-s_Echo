# 背景音乐系统说明

## 音乐文件

位置：`/public/sound/backgounrd_music/`

- `menu.wav` - 菜单界面音乐（循环播放）
- `start-level.wav` - 战斗开始音乐（播放一次）
- `battle.wav` - 战斗音乐（循环播放）
- `scream.wav` - 游戏结束音乐（播放一次）

## 播放时机

### 1. 菜单界面 (GameSetup)

- **触发**: 组件挂载时
- **音乐**: `menu.wav`
- **特点**: 循环播放，1.5秒淡入

### 2. 战斗场景 (BattleScene)

- **触发**: 战斗开始时
- **音乐序列**:
  1. `start-level.wav` 立即播放（0.8秒淡入）
  2. 2.5秒后开始淡出 `start-level.wav`（1秒淡出）
  3. 淡出完成后，淡入 `battle.wav`（1.5秒淡入）
- **特点**: start-level到battle的无缝过渡

### 3. 结束界面 (GameOver)

- **触发**: 战斗结束后
- **音乐**: `scream.wav`
- **特点**:
  - 先快速淡出当前音乐（0.5秒）
  - 0.5秒后淡入scream音乐（0.8秒淡入）
  - 不循环播放

## API 说明

### soundManager 方法

```typescript
// 播放菜单音乐
soundManager.playMenuMusic();

// 播放战斗音乐（自动处理start-level到battle的过渡）
soundManager.playBattleMusic();

// 播放游戏结束音乐
soundManager.playGameOverMusic();

// 停止所有背景音乐
soundManager.stopBackgroundMusic();

// 设置背景音乐音量 (0-1)
soundManager.setBgMusicVolume(0.3);

// 启用/禁用所有音效和音乐
soundManager.setEnabled(true / false);
```

## 音量设置

- **音效音量**: 默认 0.5 (50%)
- **背景音乐音量**: 默认 0.3 (30%)

可通过 `soundManager.setVolume()` 和 `soundManager.setBgMusicVolume()` 调整。

## 淡入淡出效果

所有音乐切换都使用平滑的淡入淡出效果：

- 淡入/淡出以50毫秒为步进
- 防止音乐突然开始/停止造成的刺耳感
- 自动管理音乐过渡，无需手动控制

## 用户控制

在游戏设置界面有"🔊 Sound Effects & Music"开关：

- 打开：播放所有音效和背景音乐
- 关闭：静音所有声音
- 重新打开时自动恢复菜单音乐
